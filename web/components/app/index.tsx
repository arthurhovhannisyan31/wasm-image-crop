import { EditorContainer } from "components/editor-container";
import { Layout } from "components/layout";

export const App = () => {
  return (
    <Layout>
      <EditorContainer />
    </Layout>
  );
};
