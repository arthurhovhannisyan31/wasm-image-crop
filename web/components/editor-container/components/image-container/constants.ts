import type { ImageDimensions } from "./types";

export const MAX_WIDTH = 600;
export const MAX_HEIGHT = 600;

export const baseImageDimensions: ImageDimensions = {
  width: MAX_WIDTH,
  height: MAX_HEIGHT
};
